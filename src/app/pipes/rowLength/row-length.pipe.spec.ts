import { RowLengthPipe } from './row-length.pipe';

describe('RowLengthPipe', () => {
  it('create an instance', () => {
    const pipe = new RowLengthPipe();
    expect(pipe).toBeTruthy();
  });
});
